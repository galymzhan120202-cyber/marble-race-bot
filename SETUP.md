# Maze Race Bot — Setup нұсқаулығы

Код пен pipeline толық дайын (`race_sim.py`, `video_gen.py`, `scheduler.py`,
`tournament_gen.py`, `.github/workflows/upload.yml`,
`.github/workflows/tournament.yml`). Бұл бот `weapon-ball-bot`-пен (және
қалған боттармен) **бірдей upload/cron архитектурада**, бірақ мазмұны
түбегейлі бөлек:

**Gemini/LLM де, дауыс (TTS) та, Pexels/stock footage те жоқ.** 6-9 квадрат
"жарысшы" (әрқайсысында кішкентай бет — екі көз + күлкі) кездейсоқ
генерацияланған лабиринтке түсіп, финишке бірінші жеткен ұтады. Жоғарыдан
көрініс (top-down), нақты `pymunk` физикасымен (гравитация жоқ, әр жарысшы
флуд-филл арақашықтық картасы бойынша келесі бағытқа өзі "жүреді" әрі
қабырғаларға/бір-біріне соғылады) — соғу дыбыстары numpy-мен синтезделеді,
нақты дыбыс файлы да жүктелмейді. Тек фон музыка сырттан алынады
(Openverse/YouTube).

## 1. Жаңа YouTube арна

1. Жаңа Google аккаунт ашыңыз (немесе қазіргі аккаунтта Brand Account құрыңыз).
2. YouTube Studio-да арнаны maze-race/marble-race нишасына сай атаумен,
   суретпен баптаңыз (мыс. "Maze Race Arena").

## 2. Google Cloud OAuth (жүктеу үшін міндетті)

1. https://console.cloud.google.com — жаңа жоба жасаңыз (мыс. `MazeRaceBot`).
2. **YouTube Data API v3**-ті қосыңыз (APIs & Services → Library).
3. **OAuth consent screen** баптаңыз (External). Бірден **"In production"**
   күйіне ауыстырыңыз (Testing емес) — Testing статусында refresh token 7
   күннен кейін мерзімі бітеді де, upload үзіліп қалады.
4. **Credentials → Create Credentials → OAuth client ID → Desktop app** жасап,
   JSON жүктеп алыңыз → `client_secrets.json` деп осы папкаға салыңыз.
5. Жергілікті бір рет `python video_gen.py` іске қосыңыз — браузерде жаңа
   арнамен логин болып, `youtube_token.json` автоматты жасалады.

**Маңызды:** OAuth логин кезінде дәл жаңа Maze Race арнаға тиесілі Google
аккаунтпен кіріңіз.

## 3. GitHub repo + Secrets

1. Жаңа бөлек GitHub repo ашыңыз (мыс. `marble-race-bot`), осы папканы push
   етіңіз.
2. Repo → Settings → Secrets and variables → Actions → төмендегі 4 Secret
   қосыңыз:
   - `MRACE_TELEGRAM_NOTIFY_TOKEN`
   - `MRACE_TELEGRAM_NOTIFY_CHAT_ID`
   - `MRACE_CLIENT_SECRETS_JSON` — `client_secrets.json` файлының толық мазмұны
   - `MRACE_YOUTUBE_TOKEN_JSON` — `youtube_token.json` файлының толық мазмұны

   Telegram-ды басқа боттарыңызбен ортақ пайдалануға болады (cron уақыттары
   15 мин ығыстырылған, соқтықпайды).

## 4. Музыка (Openverse API — автоматты, кілтсіз)

`weapon-ball-bot`-пен бірдей: [Openverse API](https://api.openverse.org/)
арқылы CC0/CC-BY лицензиялы **upbeat/energetic/arcade** фон музыка автоматты
жүктеледі, кілт керек емес. CC-BY трек түскенде атрибуция видео сипаттамасына
автоматты қосылады.

**Сақтық fallback:** `music/` папкасы қазір бос (тек `.gitkeep`). Openverse
API уақытша істемей қалса, бот дыбыссыз емес — SFX (соғу дыбыстары + финиш
чиме) әрқашан бар — бірақ фон музыкасы болмай қалады. Қаласаңыз, өзіңіз
royalty-free/CC0 2-3 трек тауып, `music/` папкасына қосып,
`music/fallback_attribution.json` файлын мына пішінде жасаңыз (тек CC0 емес
лицензия үшін керек):

```json
[
  {"file": "fallback1.mp3", "title": "...", "creator": "...", "license": "by",
   "foreign_landing_url": "..."}
]
```

## 5. Лабиринт, жарысшылар және физика — API/кілт керек емес

Ешбір баптау қажет емес. `race_sim.py`:
- **16 жарысшы скинінен** (Blacky, Sunny, Cocoa, Pine, Tango, Ghost, Sky,
  Rosy, Cherry, Minty, Grape, Coral, Lime, Slate, Amber, Ruby) кездейсоқ
  **6-9-ын** таңдайды (салмақталған), әрқайсысы флэт түсті дөңгелектелген
  квадрат + екі көз + күлкі + жүру бағытына қараған "желекше" ретінде
  салынады (нақты сурет/фото жоқ, Content ID claim жоқ);
- лабиринтті кездейсоқ DFS (recursive backtracker) алгоритмімен генерациялап,
  үстіне қосымша циклдер/балама жолдар ашады (бір ғана жол болса, "кім
  жеңеді" деген қызық жоғалады);
- финишке дейінгі flood-fill арақашықтық картасын бір рет есептеп, әр
  жарысшы соны басшылыққа алып өзі жол табады — әр жарысшыда өз "адасу
  мүмкіндігі" бар (кейде қате бұрылыс жасайды), сондықтан бірдей лабиринтте
  де жарыс әрдайым басқаша өтеді;
- `pymunk`-пен физика: гравитация жоқ, әр жарысшы келесі нүктеге "steering"
  күшімен жылжиды, қабырғаларға/бір-біріне соғылып озады. Жарысшы-жарысшы
  соқтығысулары **шыны шарик/бильярд сияқты** нақты, көрінетін серпіліспен
  болады (elasticity ≈0.92) — соғылғаннан кейін ~0.3 секунд steering күші
  әлсірейді (толығымен емес), сондықтан серпіліс экранда шынымен көрінеді,
  steering бірден "жоймайды". Бұрышта/тұйықта серпіліп қалып қоятын жарысшы
  болса, stuck-detection оны автоматты түрде бір рет итеріп жібереді;
- лабиринт біркелкі тар дәліздер емес — кейбір жерлерде бірнеше ұяшық
  біріктіріліп ашық "бөлме" жасалады (топ еркін жайылып, көбірек соқтығысу
  үшін орын болады), қалғаны қалыпты тар дәліздер;
- финишке бірінші жеткен — жеңімпаз. "FINISHED: K/N" санауышы экранда тірі
  жаңарып тұрады;
- **"3-2-1-GO!"** intro countdown, соғу флэштері, финиш попап-тары, жеңіске
  жеткенде банер + victory chime — бәрі кодпен, дайын аудио/видео файл жоқ.

Толығымен детерминистикалық: бір `seed`-пен бірдей нәтиже қайталанады
(жергілікті тексеру/debug үшін пайдалы), бірақ әр жүктеу кездейсоқ жаңа
`seed` алады.

**Белгілі шектеу (v1):** `weapon-ball-bot`-тың 26 қару/16 арена тереңдігімен
салыстырғанда, бұл бот қазір 16 жарысшы скинімен және 12 лабиринт
тақырыбымен шектелген, ешбір hazard/elimination-trap механикасы жоқ (тек
финишке жету). Форматтың тартымдылығы дәлелденсе, кейінірек оңай кеңейтіледі
(`RACER_POOL`, `MAZE_THEMES` тізімдеріне жаңа жазба қосу жеткілікті).

## 6. Tournament режимі (ұзын форматты, 16 жарысшы bracket)

Shorts-тан толығымен бөлек екінші видео түрі: `tournament_gen.py` →
`generate_tournament_video()` → `final_tournament.mp4`. Бір YouTube арна,
бір OAuth (`client_secrets.json`/`youtube_token.json`) ортақ пайдаланылады —
тек видео стилі мен жүктеу кестесі бөлек.

- **Формат:** 16 жарысшы (толық ростер) single-elimination bracket-ке
  түседі: Round of 16 (4 хит, әрқайсысында 4 жарысшы, әр хиттен **топ-2**
  өтеді) → Quarterfinals (2 хит, топ-2 өтеді) → Final (1 хит) → чемпион.
  Раундтар арасында толық bracket ағашын көрсететін экран (PIL-мен
  кодпен салынған, ешбір сурет/шрифт сырттан алынбайды) шығады.
- **Бағдары:** альбомдық (landscape) **1920×1080**, Shorts-тың тік
  1080×1920-нан бөлек. `race_sim.py`-дегі `simulate_race()` кез келген
  w/h-ке — колонналар саны видео еніне қарай автоматты есептеледі
  (`TARGET_CELL_PX`), сондықтан лабиринт әрдайым сол экран пропорциясына
  сай "созылады", тікбұрышты портрет пішінін қайталамайды.
  `TOURNAMENT_VIDEO_WIDTH/HEIGHT/FPS` арқылы `.env`-де өзгертуге болады.
  Толығымен генерациялау уақыты жаттасатын видео 30fps-те, ~7 хиттен
  жиналған нәтижесінде **3-5 минут** шамасында шығады (қатаң мақсат
  емес — форматтың табиғи ұзақтығы, жасанды қысқарту/созу жоқ).
- **Жүктеу/дыбыс:** Shorts-пен бірдей Openverse музыка + duck envelope,
  numpy SFX, retry/backoff, Telegram notify қолданылады, бірақ бөлек
  тақырып/сипаттама/hashtag шаблондарымен (`#shorts` жоқ — бұл қысқа видео
  емес, толыққанды upload).
- **Локал сынау (жүктеместен):**
  ```
  python -c "from tournament_gen import generate_tournament_video; generate_tournament_video(skip_upload=True)"
  ```
  `final_tournament.mp4` және `tournament_thumbnail.jpg` пайда болады.

### Cron кестесі — әр 4 күн сайын

`schedule` кітапханасының (`scheduler.py`) `every(4).days.at(...)` жұмысы
процесс іске қосылған сәттен бастап есептеледі (күнтізбелік бекітілген
нүкте жоқ) — сондықтан ұзақ жұмыс істейтін процесс керек, әйтпесе әр
рестарт интервалды қайта бастайды.

GitHub Actions-та таза cron синтаксисі "әр 4 күн сайын" дегенді нақты
өрнектей алмайды (белгілі бір басталу күнінен санамайды), сондықтан
`.github/workflows/tournament.yml` айдың белгіленген күндерімен
жуықтайды: `cron: '15 6 1,5,9,13,17,21,25,29 * *'` (яғни айдың 1, 5, 9,
13, 17, 21, 25, 29-күндерінде — нақты 4 күндік аралыққа өте жақын,
тек 29→1 ауысуында бір рет қысқарақ/ұзынырақ болуы мүмкін). Нақты дәл
4 күн керек болса, өз "соңғы жүктеуден бері ≥4 күн өтті ме" тексеруін
(мыс. commit-делген timestamp файл немесе GitHub Actions cache арқылы)
өзіңіз қосуыңызға болады — бұл жоба соны істемейді, тек жуықтайды.

GitHub Secrets 4-і Shorts-пен ортақ (`MRACE_CLIENT_SECRETS_JSON`,
`MRACE_YOUTUBE_TOKEN_JSON`, `MRACE_TELEGRAM_NOTIFY_TOKEN`,
`MRACE_TELEGRAM_NOTIFY_CHAT_ID`) — қосымша Secret қосудың қажеті жоқ.

## 7. Монетизация туралы маңызды ескерту

- Толығымен генерацияланған физика/визуал + Openverse CC музыка + numpy-мен
  синтезделген SFX — copyright claim тәуекелі жоқтың қасы.
- YouTube Partner Program-ге өту үшін арна **1000 жазылушы + 4000 сағат watch
  time (12 айда)** шегіне жетуі керек — бот тек контентті тұрақты шығарады.

## 8. Тексеру реті

1. `.env.example`-ды `.env` етіп көшіріп, нақты кілттермен толтырыңыз.
2. `ffmpeg`-тің жергілікті компьютерде орнатылғанын тексеріңіз (`ffmpeg -version`).
3. `python -m venv .venv` → венвты белсендіріп → `pip install -r requirements.txt`
4. Жергілікті сынау (жүктеместен): `python -c "from video_gen import generate_video; generate_video(skip_upload=True)"`
5. `final_shorts.mp4`-ты тексеріңіз (лабиринт/камера/финиш/дыбыс дұрыс па).
6. Tournament-ты бөлек сынаңыз: `python -c "from tournament_gen import generate_tournament_video; generate_tournament_video(skip_upload=True)"`
   → `final_tournament.mp4` (альбомдық, bracket экрандары дұрыс кезекте ме, соңында чемпион bracket-і толық па — тексеріңіз).
7. Нақты жүктеуді бір рет қолмен сынаңыз: `python video_gen.py` және
   `python -c "from tournament_gen import generate_tournament_video; generate_tournament_video()"`
8. Барлығы жұмыс істесе, GitHub Actions-та екі workflow-ды да
   (`upload.yml` және `tournament.yml`) `workflow_dispatch` арқылы бір рет
   қолмен іске қосып тексеріңіз.
9. Содан кейін ғана cron кестесіне сеніп қалдырыңыз.
